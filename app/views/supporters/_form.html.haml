= form_for @supporter do |f|
  - if @supporter.errors.any?
    #error_explanation
      %h2= "#{pluralize(@supporter.errors.count, "error")} prohibited this supporter from being saved:"
      %ul
        - @supporter.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :email
    = f.text_field :email
  .field
    = f.label :phone
    = f.text_field :phone
  .actions
    = f.submit 'Save'
