%h1 Listing supporters

%table.table.table-striped
  %tr
    %th Name
    %th Email
    %th Phone
    %th

  - @supporters.each do |supporter|
    %tr
      %td= supporter.name
      %td= supporter.email
      %td= supporter.phone
      %td
        %a= link_to 'Show', supporter
        %a= link_to 'Edit', edit_supporter_path(supporter)
        %a= link_to 'Destroy', supporter, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Supporter', new_supporter_path
